# دليل البدء السريع - لوحة التحكم للموزعين

## 🚀 التشغيل السريع

### الخطوات الأساسية:
1. **تأكد من تحديث التبعيات**:
   ```bash
   flutter pub get
   ```

2. **تشغيل التطبيق**:
   ```bash
   flutter run
   ```

3. **الوصول للوحة التحكم**:
   - سجل دخول كموزع
   - اذهب للقائمة الجانبية 
   - اختر "لوحة التحكم"

## 📊 المميزات الرئيسية

### 🏠 الشاشة الرئيسية
- **إحصائيات سريعة**: إجمالي المنتجات، العروض، المشاهدات، التقييم
- **مؤشر النمو**: نسبة النمو الشهري مع مؤشرات بصرية

### ⚡ الإجراءات السريعة  
- إضافة منتج جديد
- إضافة عرض مؤقت
- إدارة الأدوات الجراحية
- إدارة المستلزمات البيطرية

### 📈 التحليلات
- **رسم بياني للأداء**: مشاهدات وطلبات شهرية
- **أفضل المنتجات**: ترتيب حسب المشاهدات
- **المنتجات الأحدث**: آخر ما تم إضافته

### 🔔 التنبيهات الذكية
- منتجات قاربت على الانتهاء
- مخزون منخفض  
- فرص تسويقية

### 🗺️ التوزيع الجغرافي
- إحصائيات العملاء حسب المحافظات
- عدد الطلبات لكل منطقة

## 🔧 المتطلبات التقنية

### المكتبات المستخدمة:
- `fl_chart: ^0.69.0` للرسوم البيانية
- `flutter_riverpod` لإدارة الحالة
- `easy_localization` للترجمة
- `supabase_flutter` لقاعدة البيانات

### البيانات المطلوبة:
- جداول: `products`, `offers`, `surgical_tools`, `vet_supplies`
- أعمدة مهمة: `user_id`, `views`, `created_at`, `expiry_date`

## 🎨 التخصيص

### تعديل الألوان:
```dart
// في dashboard_stats_card.dart
color: Colors.blue,  // غير اللون حسب التفضيل
```

### إضافة إحصائيات جديدة:
1. أضف البيانات في `dashboard_repository.dart`
2. حدث `dashboard_stats.dart`
3. أضف ويدجت جديد في `widgets/`

## 🔄 التحديث والصيانة

### تحديث البيانات:
- اسحب الشاشة لأسفل للتحديث
- البيانات تتحدث تلقائياً عند فتح الصفحة

### إضافة ميزة جديدة:
1. أنشئ ويدجت جديد في `widgets/`
2. أضفه للصفحة الرئيسية `dashboard_page.dart`
3. حدث الترجمات في `ar.json`

## 🚨 استكشاف الأخطاء

### مشاكل شائعة:
- **لا تظهر بيانات**: تأكد من وجود بيانات في قاعدة البيانات
- **خطأ في الرسم البياني**: تحقق من مكتبة `fl_chart`
- **مشاكل الترجمة**: تأكد من صحة ملف `ar.json`

### رسائل الخطأ:
```dart
// في حالة عدم وجود بيانات
return DashboardStats.empty();

// في حالة خطأ في التحميل  
error: (error, stack) => Center(child: Text('خطأ في التحميل'))
```

## 📞 الدعم

للمساعدة أو الاستفسارات:
- راجع `DISTRIBUTOR_DASHBOARD_GUIDE.md` للتفاصيل الكاملة
- تحقق من console للأخطاء
- اختبر على أجهزة مختلفة

---

## ✅ قائمة المراجعة السريعة

- [ ] تم تشغيل `flutter pub get`
- [ ] التطبيق يعمل بدون أخطاء
- [ ] يمكن الوصول للوحة التحكم
- [ ] الإحصائيات تظهر بشكل صحيح
- [ ] الرسوم البيانية تعمل
- [ ] التنبيهات تظهر
- [ ] يمكن التنقل للصفحات الأخرى

**🎉 تهانينا! لوحة التحكم جاهزة للاستخدام**